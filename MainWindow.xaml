<Window x:Class="TheScheduler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheScheduler.ViewModels"
        xmlns:utils="clr-namespace:TheScheduler.Utils"
        xmlns:Components="clr-namespace:TheScheduler.Components"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        WindowStyle="None"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        ResizeMode="CanResize"
        Background="#FF1E1E1E"
        Title="TheScheduler"
        Height="720" Width="1280">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            ResizeBorderThickness="10"
            CaptionHeight="32"
            CornerRadius="0"
            GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <DockPanel Background="Transparent">
        <!-- 상단 메뉴바 -->
        <Grid DockPanel.Dock="Top" >
            <DockPanel Margin="5">                
                <Menu DockPanel.Dock="Left" shell:WindowChrome.IsHitTestVisibleInChrome="True" ItemContainerStyle="{StaticResource NoBorderMenuItemStyle}">
                    <MenuItem Header="{utils:Loc MenuMain}" Command="{Binding Home_ClickCommand}" />
                    <MenuItem Header="{utils:Loc MenuMembers}" Command="{Binding Member_ClickCommand}"/>
                    <MenuItem Header="{utils:Loc MenuShifts}" Command="{Binding Shift_ClickCommand}"/>
                    <MenuItem Header="{utils:Loc MenuPrint}" Command="{Binding PrintCommand}"/>
                </Menu>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <ComboBox ItemsSource="{Binding AvailableCultures}" 
                              SelectedItem="{Binding SelectedCulture}"
                              VerticalAlignment="Center" Margin="0,0,10,0">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NativeName}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Content="―" Style="{StaticResource WindowControlButton}" Click="Minimize_Click"/>
                    <Button Content="□" Style="{StaticResource WindowControlButton}" Click="Maximize_Click"/>
                    <Button Content="X" Style="{StaticResource WindowControlButton}" Click="Close_Click"/>
                </StackPanel>
            </DockPanel>
        </Grid>


        <Grid>
            <!-- 메인 콘텐츠 -->
            <ContentControl Content="{Binding MainContent}"/>
            
            <!-- 근무 모달 -->
            <Border Visibility="{Binding IsShiftDialogOpen, Converter={StaticResource UniversalConverter}}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="#BA232323"
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.ColumnSpan="3">
                <Components:ShiftManagementDialog DataContext="{Binding ShiftVM}"/>
            </Border>
        </Grid>
    </DockPanel>

</Window>
