 <UserControl x:Class="TheScheduler.Components.ScheduleDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheScheduler.Components"
             xmlns:utils="clr-namespace:TheScheduler.Utils"
             mc:Ignorable="d">

    <Grid Margin="10,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Center" Text="{utils:Loc ScheduleDialog_SelectShift}" FontSize="17" FontWeight="Bold"/>
        <ComboBox VerticalAlignment="Center" Grid.Column="1" ItemsSource="{Binding Shifts}"
                  Margin="20,0,20,0"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Schedule.ShiftId, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="25" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="20" Height="20" Background="{Binding ShiftColor, Converter={StaticResource EnumToBrushConverter}}" BorderBrush="Gray" BorderThickness="1" CornerRadius="3"/>
                        <TextBlock Grid.Column="2" Text="{Binding Name}"/>
                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
